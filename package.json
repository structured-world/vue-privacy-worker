{
  "name": "vue-privacy-worker",
  "description": "Cloudflare Worker companion for @structured-world/vue-privacy - GDPR consent storage",
  "version": "1.1.0",
  "private": true,
  "packageManager": "yarn@4.12.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit && echo 'Type check passed'"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.0",
    "@cloudflare/workers-types": "^4.20241230.0",
    "typescript": "^5.7.2",
    "vitest": "~2.1.0",
    "wrangler": "^4.61.1"
  }
}
